{%-set exception %}{{resolver.cpp_get_lib_ns() | join('::')}}::JsonSchemaException{%endset%}
/*! {{Name}} is a wrapper around "null".
 {%-if schema.description %}
 * {{schema.description}}
 {%-endif%}
 */
class {{Name}}
{
public:
    /*! Constructor
     */
    {{Name}}();

    virtual ~{{Name}}() = default;

    /*! \fn bstd::nullopt_t Get() const
     * \brief returns std::nullopt_t which represents "null"
     * This is pointless because it never returns anything other than std::nullopt
     * \returns std::nullopt
     */
    std::nullopt_t Get() const;

    /*! \fn {{Name}} FromJson(const nlohmann::json& json)
     * \brief Deserializes a JSON "null" value into a new instance of {{Name}}.
     * \param json is the json value which must be of "null" type.
     * \throw {{exception}} If the JSON isn't a null
     * \returns {{Name}}
     */
    static {{Name}} FromJson(const nlohmann::json& json);

    /*! \fn ToJson(nlohmann::json& value)
     * \brief Sets 'value' to null
     * \param value is the json value which will be modified to contain the serialization
     */
    void ToJson(nlohmann::json& value) const;

    /*! Sets a string handle associated with this {{Name}} instance.
     * This gets called by a parent object after creating an instance that is used for an object's property.
     * \param handle is the string name.
     */
    void SetHandle(const std::string& handle);

    /*! Gets the string handle associated with this {{Name}} instance.
     * This is often the property name used in a JSON-object parent.
     * It may be empty.
     * \returns the handle string
     */
    std::string GetHandle() const;
private:
    std::string _handle;
};